dependencies {
    implementation 'org.springframework.cloud:spring-cloud-starter-netflix-eureka-client'
    implementation(project(":components:module:gateway"))
    implementation(project(":components:support:swagger"))
}

bootJar {
    enabled = false
}

jar {
    enabled = true
}